name: salmon-rnaseq
channels:
  - bioconda
  - conda-forge
  - defaults

dependencies:
  - python=3.10.13
  - salmon=1.10.1
  - fastqc=0.12.1
  - multiqc  # REMOVE – keep placeholder for pip step
  - samtools=1.18
  - pip       # ensure pip exists

  # Remove problematic tools from conda (fastq-dl, multiqc)
  # They will be installed via pip instead.

  - pip:
      - fastq-dl
      - multiqc
