name: salmon-rnaseq
channels:
  - bioconda
  - conda-forge
  - defaults

dependencies:
  # Python pin (critical for solver compatibility)
  - python=3.10

  # Core RNA-seq tools
  - salmon
  - fastq-dl
  - fastqc
  - multiqc

  # Useful essentials
  - pandas
  - numpy
  - matplotlib
  - pip

  
